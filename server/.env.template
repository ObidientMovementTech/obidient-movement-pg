# Server Environment Variables Template
# Copy this file to .env and fill in your actual values

# Database Configuration
DB_HOST=109.176.207.91
DB_PORT=5432
DB_NAME=dev21
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# JWT Configuration
JWT_SECRET=your_very_secure_random_jwt_secret_here
SESSION_SECRET=your_very_secure_session_secret_here

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/onboarding/google/callback
GOOGLE_LOGIN_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Frontend URL
FRONTEND_URL=http://localhost:5173
CLIENT_URL=http://localhost:5173

# Server Configuration
PORT=5000
NODE_ENV=development

# AWS S3 Configuration (for profile images)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=your_aws_region
AWS_S3_BUCKET=your_s3_bucket_name

# Email Configuration (Optional)
# Add your email service configuration here

# ==========================================
# ONBOARDING SYSTEM SETUP INSTRUCTIONS
# ==========================================
#
# 1. Google OAuth Setup:
#    - Go to https://console.cloud.google.com/
#    - Create a new project or select existing project
#    - Enable Google+ API
#    - Go to "Credentials" → "Create Credentials" → "OAuth 2.0 Client ID"
#    - Application type: Web application
#    - Authorized redirect URIs:
#      * http://localhost:5000/auth/onboarding/google/callback (development)
#      * http://localhost:5000/auth/google/callback (development login)
#      * https://your-domain.com/auth/onboarding/google/callback (production)
#      * https://your-domain.com/auth/google/callback (production login)
#    - Copy Client ID and Client Secret to this file
#
# 2. Generate Secrets:
#    - JWT_SECRET: Run `openssl rand -base64 32` in terminal
#    - SESSION_SECRET: Run `openssl rand -base64 32` in terminal
#
# 3. Database:
#    - Migration has already been run
#    - Ensure connection details are correct
#
# 4. Frontend URL:
#    - Development: http://localhost:5173
#    - Production: https://your-frontend-domain.com
#
# 5. Test the setup:
#    - npm run dev (start server)
#    - Create an onboarding token via admin endpoint
#    - Access the onboarding URL in browser
